services:
  - type: web
    name: recycling-ai-backend
    env: python
    plan: free

    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app --timeout 0"

    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
